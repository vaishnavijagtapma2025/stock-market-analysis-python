# Source Probes

For the milestone, every main data source should have a tiny proof that it works.

Good examples:

- one row from an API response
- one row from a CSV or Excel file
- one screenshot of a successful authenticated fetch
- one tiny JSON or Markdown note that shows the fetch worked

These are not full analyses. They are just small proofs that the source is real and reachable.
